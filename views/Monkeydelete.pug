extends layout

block content
    h1= title

    if (toShow == null)
        h2 No instance to delete
    else
        - let Monkeyid = toShow._id
        - let Monkeyname = toShow.Monkey_name
        - let Monkeyage = toShow.Monkey_gender
        - let Monkeysalary = toShow.Monkey_age
        - let actionid = "/resource/Monkeys/" + Monkeyid
        script let actionid = "#{actionid}"
        div
            div.row
                div(class='MonkeyAttr mr-3') Monkey ID is:
                div #{Monkeyid}
            div.row
                div(class='MonkeyAttr mr-3') Monkey Name  is:
                div #{Monkeyname}
            div.row
                div(class='MonkeyAttr mr-3') Monkey gender is:
                div #{Monkeygender}
            div.row
                div(class='MonkeyAttr mr-3') Monkey age is:
                div #{Monkeyage}

        button(class="btn btn-danger m-2" onclick="deleteOnid()") Delete
        button(class="btn btn-secondary m-2" onclick="cancel()") Cancel

    script.
        function cancel() {
            // Go back to the home page for the Monkey collection
            window.location.href='/resource/Monkeys'
        }
    script.
        function deleteOnid() {
            console.log("starting delete")
            // delete first
            fetch(actionid, {
                method: 'DELETE'      // Don't need a body for the delete
            }).then((resp) => {
                return resp.json(); // or resp.text() or whatever the server sends
            }).then((body) => {
                alert("Delete succeded")
                window.location.href='/resource/Monkeys'
                // TODO handle body
            }).catch((error) => {
                // TODO handle error
                alert("Delete failed " + error )
            });
        }